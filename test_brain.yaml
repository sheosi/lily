include: some_file.yaml
translation: translations.ftl

say_hello:
  signals:
    order: hello there

  actions:
    say: say_hello_i18n # This is a fluent reference

lily_start:
  actions:
    say: Lily lista para la acción

init_reco:
  actions:
    say: Dime
